import * as Device from "expo-device";import * as Notifications from "expo-notifications";import Constants from "expo-constants";import { Platform } from "react-native";import { supabase } from "./supabase";
Notifications.setNotificationHandler({handleNotification:async()=>({shouldShowBanner:true,shouldShowList:true,shouldPlaySound:true,shouldSetBadge:true})});
export async function registerForPush(userId:string){if(!Device.isDevice)return null;let permission=(await Notifications.getPermissionsAsync()).status;if(permission!=="granted")permission=(await Notifications.requestPermissionsAsync()).status;if(permission!=="granted")return null;if(Platform.OS==="android")await Notifications.setNotificationChannelAsync("tasks",{name:"Task updates",importance:Notifications.AndroidImportance.HIGH});const projectId=process.env.EXPO_PUBLIC_EAS_PROJECT_ID||Constants.expoConfig?.extra?.eas?.projectId;const token=(await Notifications.getExpoPushTokenAsync({projectId})).data;await supabase.from("push_tokens").upsert({user_id:userId,token,device_type:Platform.OS},{onConflict:"token"});return token;}
